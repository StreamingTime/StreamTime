name: Run elm checks

on:
  push:
    branches-ignore:
      - main
  workflow_call:

env:
  COMMIT_SHA: ${GITHUB_SHA}

jobs:

 test:
    uses: StreamingTime/StreamTime/.github/workflows/elm-test.yml@main
 review:
     uses: StreamingTime/StreamTime/.github/workflows/elm-review.yml@main
 format:
     uses: StreamingTime/StreamTime/.github/workflows/elm-format.yml@main
